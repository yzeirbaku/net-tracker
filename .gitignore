# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Env
.env
*.env.local

# Build artifacts
dist/
build/
*.egg-info/

# Local-only superpowers specs/plans — kept on disk, never committed
docs/superpowers/

# Claude Code local state — but DO track project skills.
# Use `.claude/*` (not `.claude/`) so the negation below actually works;
# excluding the whole dir would mean git never scans inside it.
.claude/*
!.claude/skills/

# Dev-stack runtime state (pids.json, log files)
.dev/

# Superpowers brainstorm companion artifacts (HTML mockups, session state)
.superpowers/
